## Prerequisites

[Node.js](https://nodejs.org/).

## Inentory table (state manipulation example)

sample apllication build using react JS, shows inventory table using mock data.
performs  state manipulation on user input


### Run the application

Start the application back-end by running the following command:

```
$ npm install
$ node server.js 
```
